# =====================================================================
#  COSMOS — Solar System 3D (Three.js) — self-hosted stack
#  App static + API server (auth + AI gateway)
#  docker compose up -d --build
# =====================================================================
services:
  cosmos:
    build: .
    image: cosmos-web:latest
    container_name: cosmos-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:80"   # chỉ lắng nghe localhost — truy cập thật qua proxy/SSH tunnel [2]
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - webnet

  api:
    build: ./server
    image: cosmos-api:latest
    container_name: cosmos-api
    restart: unless-stopped
    environment:
      - PORT=3000
      - JWT_SECRET=${JWT_SECRET:?Hãy đặt JWT_SECRET trong file .env (xem .env.example)}
      - SETUP_TOKEN=${SETUP_TOKEN:-}
    volumes:
      - api_data:/app/data
    # hardening (đề xuất đánh giá): không nâng quyền, bỏ capabilities
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - webnet

  # ====== bật block này khi có tên miền + Let's Encrypt ======
  # cosmos-proxy:
  #   image: nginx:1.27-alpine
  #   container_name: cosmos-proxy
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginxconf/proxy.conf:/etc/nginx/conf.d/proxy.conf:ro
  #     - ./certbot/conf:/etc/letsencrypt:ro
  #     - ./certbot/www:/var/www/certbot
  #   depends_on:
  #     - cosmos
  #   networks:
  #     - webnet
  #
  # certbot:
  #   image: certbot/certbot
  #   container_name: cosmos-certbot
  #   volumes:
  #     - ./certbot/conf:/etc/letsencrypt
  #     - ./certbot/www:/var/www/certbot
  #   entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
  #   networks:
  #     - webnet

networks:
  webnet:
    driver: bridge

volumes:
  api_data:
