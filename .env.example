# Copy thành .env rồi điền giá trị thật. KHÔNG commit .env lên git.
# Sinh chuỗi ngẫu nhiên: openssl rand -base64 32
JWT_SECRET=
# Optional: bảo vệ endpoint /api/setup lần đầu (để trống = chỉ khóa sau khi admin tồn tại)
SETUP_TOKEN=
